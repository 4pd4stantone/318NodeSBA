<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leave Review</title>
    <style>
         nav {
            display: flex;
            border-bottom: 1px black solid;
            background-color: rgb(117, 0, 0);
            
        }
        .nav-bar {
            flex-grow: 1;
            width: 100px;
            text-align: center;
            height: 30px;
            margin-top: 8px;
        }
        a {
            font-size: 20px;
            text-decoration: none;
            color: whitesmoke;
            font-weight: bolder;
        }
        .social-name {
            /* outline: 1px red solid; */
            text-align: center;
        }
        label, input, .form {
            display: block;
            text-align: center;
            margin:  10px auto 10px auto;
        }

    </style>
</head>
<body>
    <nav>
        <div class="nav-bar">
            <a href="/">Home</a>
        </div>
        <div class="nav-bar">
            <a href="/socials">Socials</a>
        </div>
        <div class="nav-bar">
            <a href="/comments">Ratings/Comments</a>
        </div>
    </nav>
    <main>
        <div style="text-align: center; margin-top: 50px; font-size: 20px;">
            <h1 style="text-decoration: underline;">Latin Social Ratings & Comments</h1>
        </div>
        <form action="">
                <h2 class="form">Leave a Review:</h2>
                <label for="Name" >Social:</label>
                <select name="Dance Style" id="" class="form" style="margin: 20px auto 20px auto; width: 150px;">
                    <% if (socials.length > 0) { %>
                <% socials.forEach(socials => { %>
                    <br>
                        <option value=""><%= socials.name %></option>

                <% }) %>
                <% } else { %>
                    <option value="">No Socials</option>
                <% } %>
                </select>
                <label for="Rating">Rating:</label>
                <input type="number" name="rating" id="" max="10" min="1" style="width: 50px;" required>
                <label for="content">Comment:</label>
                <textarea name="" id="" class="form" style="width: 500px; height: 100px;" required></textarea>
                 <button class="form" style="width: 100px; margin-top: 30px; margin-bottom: 50px;">Submit</button>
            </form>
        
       
    </main>
    <footer>
    </footer>
    
</body>
</html>